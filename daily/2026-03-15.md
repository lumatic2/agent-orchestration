# 2026-03-15 (토)

## 오늘 포커스
- GitHub 트렌드 (2026-03-14 리포트) 적용
- 오케스트레이션 시스템 개선 (gsd-2 / CLI-Anything / OpenClaw 패턴)

## 완료
- [x] [중] 내가 쓰는 AI 팁 총정리 문서화 #크리에이티브

## 메모
### 이번 세션에서 한 일
- GitHub 트렌드 TOP 항목 적용: claude-statusline 설정, nah 보안가드 설치 (guard.sh 이중 보호)
- gsd-2 패턴 적용: task_brief.md에 Context Budget + Stop Triggers 추가, 태스크 생성 시 progress.md 자동 생성
- CLI-Anything 패턴 적용: --status --json, schema --json 시스템 커맨드 포함
- SHARED_MEMORY.md 구조 개선: 673줄 → 67줄, context/ 디렉토리에 5개 프로젝트 파일 분리
- 블루프린트 시스템 구축: blueprints/ (slides/feature-dev/research-to-vault) + run_blueprint.py + orchestrate.sh run 서브커맨드
- OpenClaw 오픈소스 분석 (Gemini): 정체 확인 + 참고 패턴 3개 도출 (대시보드/블루프린트/스킬진화)
- T058/T080 큐 오기록 수정, luma3 CLI 불필요 결론

### 블로커
- 없음

### 다음 세션에 이어할 것
- 슬라이드 블루프린트 실전 테스트: orchestrate.sh run blueprints/slides.yaml --var topic=실제주제
- OpenClaw 대시보드 패턴 (프론트엔드 작업 필요)
- orchestrate.sh 태스크별 context 파일 자동 주입 (동적 컨텍스트 로딩)

---

### 이번 세션에서 한 일 (오케스트레이션 미활용 영역 전수 점검)
- T115/T118 큐 정리, MoviePy API v2 호환 패치 (video_creator.py) + GitHub push
- Google Workspace MCP 라우팅 룰 ROUTING_TABLE.md + adapter 추가 (Gmail/Calendar/Sheets/Drive → Claude MCP 직접)
- orchestrate.sh vault_check() 추가 — Gemini 호출 전 7일 캐시 검색, --force 강제 재실행
- Gemini Pro 판단 테이블 구체화, 멀티모달 라우팅 정정 (이미지→Claude Read, 영상→API 수동)
- Nanobanana 라우팅 수정 (API 과금으로 미사용 → Gemini 웹 수동 생성으로 정정)
- vault → SHARED_MEMORY 승격 기준 추가, 전체 sync.sh 배포

### 블로커
- M1 ~/Desktop TCC 권한으로 SSH에서 content-automation 디렉토리 접근 불가 → M1 직접 접근 시 git pull 필요

### 다음 세션에 이어할 것
- M1에서 `cd ~/Desktop/content-automation && git pull` 실행 (MoviePy 패치 반영)
- content-automation --dry-run 전체 파이프라인 테스트

---

### 이번 세션에서 한 일 (AI 팁 총정리 문서화)
- vault/30-projects/ai-tips/ 에 braindump → outline → master draft(v0.8) → blog(v0.2) 완성
- 4장 GCP Secret Manager 심화 박스 적용, 마무리 수미상관("나는 개발자가 아니다") 추가
- 2→3장 브릿지 문장 추가, 전체 흐름 점검 완료
- 바탕화면에 AI팁_총정리.md + AI팁_총정리.pdf 생성 (Edge 헤드리스 변환)

### 블로커
- 없음

### 다음 세션에 이어할 것
- 블로그(브런치/네이버) 실제 포스팅
- PDF 공유 채널 결정 (SNS, 지인 공유 등)

---

### 이번 세션에서 한 일 (텔레그램 알림 시스템 전수 개선)
- 투자봇(#5): 실시간 환율(Yahoo Finance), notify_daily_open/close 추가, 현재가·금액 표시, daily_open(09:00)/daily_close(15:35) 스케줄 추가, /investment 스킬 생성
- content-automation(#6): HTML parse_mode + 해시태그 표시 + 플랫폼 이모지 + /content 스킬 생성
- 식재료 인터랙티브봇(#10): /status·/low·/fridge·/list HTML 포맷 + Notion 클릭 URL
- claude-telegram-bot(#9): 정상 작동 확인 — 인프라 수정 불필요
- signal_bot(#11): 파일 없음 확인

### 블로커
- M1 Desktop TCC 권한 → content-automation SSH 직접 접근 불가 (~/projects/에서 관리)

### 다음 세션에 이어할 것
- 투자봇 daily_open 09:00 KST 실제 수신 확인
- content-automation --dry-run 전체 파이프라인 테스트

---

### 이번 세션에서 한 일 (슬라이드·문서 파이프라인 완성)
- 슬라이드 Option B 구현: 8개 HTML 컴포넌트 + inject-slides.py + slides.sh (Codex T125)
- SVG 아이콘 라이브러리 24개 구축 (icons.json, accent-1 색 적용), before_after 슬라이드 타입 추가
- E2E 테스트: `slides.sh "와인의 역사" 9` → CHK 9/9 PASS, PDF 생성 성공
- 문서 파이프라인 구축: 7개 섹션 컴포넌트 + inject-docs.py + render-docs.sh + docs.sh (Codex T129)
- docs 레이아웃 수정: 섹션마다 페이지 강제 → cover 단독 + 나머지 자연 흐름으로 개선
- `docs.sh --word` 추가: pandoc으로 PDF + DOCX 동시 생성
- SHARED_MEMORY 업데이트: slides.sh / docs.sh 사용법 명시

### 블로커
- 없음

### 다음 세션에 이어할 것
- 회사소개서 PDF 제작 (`docs.sh "플랜바이 회사소개서" proposal`)
- content-automation M1 git pull + --dry-run 테스트
